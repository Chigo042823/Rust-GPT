// pub struct EncoderConfig {
//     n_embed: usize, //numerical embeddings per token (d_model)
//     n_heads: usize,
//     dropout: f32,   
//     batch_size: usize,
//     max_seq_len: usize,
//     ff_size: usize, //size of hidden feed forward network
//     n_layers: usize // number of encoders 
// }

//GPT is a decoder only model 
//No need for encoder